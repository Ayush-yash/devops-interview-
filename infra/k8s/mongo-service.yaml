apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: devops-platform
  labels:
    app: mongo
spec:
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # Headless Service for StatefulSet stable DNS resolution
  selector:
    app: mongo
